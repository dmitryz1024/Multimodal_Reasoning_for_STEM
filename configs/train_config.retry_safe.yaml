# Conservative retry config intended for recovering from unstable or collapsed SFT.
# Lower LR, shorter sequences, and a smaller effective working set make it safer
# to verify that fine-tuning actually improves over zero-shot before scaling up.

model:
  name: "HuggingFaceTB/SmolVLM-256M-Instruct"
  use_lora: true
  lora_r: 16
  lora_alpha: 32
  lora_dropout: 0.05
  lora_target_modules:
    - "q_proj"
    - "v_proj"
    - "k_proj"
    - "o_proj"
  load_in_4bit: true
  load_in_8bit: false

data:
  primary_dataset: "linxy/LaTeX_OCR"
  primary_subset: "human_handwrite"
  secondary_dataset: "deepcopy/MathWriting-human"
  use_secondary: false
  secondary_sample_size: 3000
  max_samples_train: null
  max_samples_val: null
  test_subset: "human_handwrite"

training:
  output_dir: "./checkpoints"
  num_epochs: 2
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 1
  gradient_accumulation_steps: 8
  learning_rate: 5.0e-5
  weight_decay: 0.01
  warmup_ratio: 0.05
  lr_scheduler_type: "cosine"
  max_grad_norm: 1.0
  fp16: false
  bf16: true
  gradient_checkpointing: true
  save_strategy: "epoch"
  eval_strategy: "epoch"
  logging_steps: 5
  save_total_limit: 2
  load_best_model_at_end: true
  metric_for_best_model: "eval_loss"
  greater_is_better: false
  seed: 42

preprocessing:
  max_length: 384
  image_size: 384
